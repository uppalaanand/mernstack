### Steps to create Backend
    1. Generate package.json
        npm init -y                 -npm -> node package manager

    2. Create HTTP Server
        a. Install & import express module
            npm install                     - used to install any modele
            npm install express               - it will install express module & stores in node_modules
        
        b. Import express module

        c. Install nodemon          --- Instent Restarts the server
            npm i nodemon



HTTP Req types
    GET  --- Read resources
    POST  --- Create a new resource
    PUT   --- Update a resource
    DELETE   ---  Delete a resource

    https://    127.0.0.1   :3000
                    |       |
                ip adrs     Port number


session 2

REST End point rules
    Rule 1 : Use Nouns, Not Verbs
        GET /users       --- Get All users
        POST /users      
        DELETE /users
    Rule 2 : User lowercases Or 

